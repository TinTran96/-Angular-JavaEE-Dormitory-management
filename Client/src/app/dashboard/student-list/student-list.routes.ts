import { Route } from '@angular/router';

import { StudentListComponent } from './index';

export const StudentListRoutes: Route[] = [
  {
    path: 'student-list',
    component: StudentListComponent
  }
];
